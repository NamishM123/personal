# Client-side. Included in the browser bundle — only use with a personal-use key.
VITE_GROQ_API_KEY=gsk_...

# Server-side (Vercel/Netlify). NOT exposed to the browser.
# Origin your frontend is served from. Use "*" while developing.
ALLOWED_ORIGIN=https://your-app.vercel.app

# Optional — only needed if you use the token-based Canvas REST API path
# (api/canvas.ts). If you only use the iCal path (api/ical.ts), leave these blank.
CANVAS_BASE_URL=https://canvas.your-school.edu
CANVAS_TOKEN=your_canvas_personal_access_token
